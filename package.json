{
  "private": true,
  "description": "",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "example": "npm run -w examples/demo-1 dev",
    "example:prod": "NODE_ENV=production npm run -w examples/demo-1 dev",
    "build": "turbo run build --filter './packages/core'",
    "clean": "find . -name dist -type d -not -path './node_modules/*' -exec rm -rf {} +",
    "install-links": "npm install --install-links"
  },
  "license": "MIT",
  "devDependencies": {
    "tsup": "^6.6.3",
    "tsx": "^3.12.6",
    "turbo": "^1.9.3"
  }
}
